using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class LTscale: MonoBehaviour
{
    public float scaleSpeed = 10;

    void Start()
    {
    //    LeanTween.scale(gameObject.GetComponent<Transform>(Scale), new Vector3 (10f, 10f, 0f) , scaleSpeed);
    }
}
